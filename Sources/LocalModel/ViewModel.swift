import Foundation
import Observation
import SkipFuse

/// A logger for the LocalModel module.
let logger: Logger = Logger(subsystem: "local.model", category: "LocalModel")

/// The Observable ViewModel used by the application.
@Observable public class ViewModel {
   

    public init() {
    }
    
}

